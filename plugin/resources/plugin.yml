name: AdminDm
version: 1.3.0
main: eval.dm.AdminDmPlugin
api-version: "1.21"
description: On-join research-deployment disclosure and a block-change observer that emits structured lines into latest.log.
commands:
  info:
    usage: /info
    description: Re-display the research-deployment disclosure for this server.
