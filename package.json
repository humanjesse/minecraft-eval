{
  "name": "minecraft-eval",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Longitudinal values-stability eval: model as Minecraft server admin",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "latest",
    "@earendil-works/pi-ai": "latest",
    "chokidar": "^4.0.0",
    "dotenv": "^16.4.0",
    "rcon-client": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
